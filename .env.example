# API Configuration
# Base URL for the backend API (without trailing slash)
# Example: http://localhost:8000 or https://api.example.com
NEXT_PUBLIC_BASE_URL=http://localhost:8000

# Node Environment
# Options: development, production, test
# This is usually set automatically by Next.js, but you can override it
NODE_ENV=development
